<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>OTP Verification</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="flex flex-col gap-6 justify-center items-center m-auto mt-52 mb-52">
    <form class="flex flex-col gap-6" action="/verify-otp-login" method="post">
        <div class="flex m-auto justify-center items-center gap-2">
            <h3>Email : </h3>
            <input class="border-2 border-black rounded-md p-1 " name="email" value="<%= email %>">
        </div>
        <input class="border-2 border-black rounded-md p-1" type="text" name="otp" id="otp" placeholder="Enter OTP" required>
        <button class="p-1 border-2 border-black rounded-lg w-40 m-auto bg-blue-400" type="submit">Verify OTP</button>
    </form>

    <div class="m-auto">
        error
        <% if (typeof error !== 'undefined') { %>
            <p class="text-red-500"><%= error %></p>
        <% } %>
        success
        <% if (typeof success !== 'undefined') { %>
            <p class="text-green-500"><%= success %></p>
        <% } %>
    </div>
    </div>
</body>
</html> -->



<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MoneyMate - Your stress-free solution for group expenses.</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="flex flex-col gap-6 justify-center items-center m-auto mt-52 mb-52">
        <form class="flex flex-col gap-6" action="/verify-otp-login" method="post">
            <div class="flex m-auto justify-center items-center gap-2">
                <h3>Email : </h3>
                <input class="border-2 border-black rounded-md p-1" name="email" value="<%= email %>" readonly>
            </div>
            <input class="border-2 border-black rounded-md p-1" type="text" name="otp" id="otp" placeholder="Enter OTP" required>
            <button class="p-1 border-2 border-black rounded-lg w-40 m-auto bg-blue-400" type="submit">Verify OTP</button>
        </form>

        <div class="m-auto">
            <% if (typeof error !== 'undefined') { %>
                <p class="text-red-500"><%= error %></p>
            <% } %>
            <% if (typeof success !== 'undefined') { %>
                <p class="text-green-500"><%= success %></p>
            <% } %>
        </div>
    </div>
    <script src="/public/js/app.js"></script>

</body>
</html> -->



<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MoneyMate - Verify OTP</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 min-h-screen flex items-center justify-center">

    <div class="bg-white p-8 rounded-lg shadow-lg w-full max-w-md">
        <h1 class="text-2xl font-semibold text-center text-gray-800 mb-6">Verify OTP</h1>
        
        <form class="space-y-6" action="/verify-otp-login" method="post">
            
            <!-- Email (read-only) -->
            <div class="flex justify-between items-center">
                <label for="email" class="text-lg font-medium">Email:</label>
                <input class="border-2 border-gray-300 rounded-md p-2 w-full focus:outline-none focus:border-blue-500" 
                       name="email" value="<%= email %>" readonly>
            </div>

            <!-- OTP Input -->
            <input class="border-2 border-gray-300 rounded-md p-3 w-full focus:outline-none focus:border-blue-500" 
                   type="text" name="otp" id="otp" placeholder="Enter OTP" required>

            <!-- Submit Button -->
            <button class="w-full bg-blue-500 text-white py-2 rounded-lg hover:bg-blue-600 transition duration-300" 
                    type="submit">Verify OTP</button>
        </form>

        <!-- Error/Success Messages -->
        <div class="mt-4 text-center">
            <!-- Error Message -->
            <% if (typeof error !== 'undefined') { %>
                <p class="text-red-500"><%= error %></p>
            <% } %>
            <!-- Success Message -->
            <% if (typeof success !== 'undefined') { %>
                <p class="text-green-500"><%= success %></p>
            <% } %>
        </div>
    </div>

    <script src="/public/js/app.js"></script>

</body>
</html>
