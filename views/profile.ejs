<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MoneyMate - Your stress-free solution for group expenses.</title>
</head>
<body>
    <div>
        <h1>Profile</h1>
        <div>

            <img style="max-width: 25px; max-height: 25px;" src="/images/uploads/<%= user.profilepic %>" alt="Profile Picture">

            <h2><%= user.username %></h2>
            <h3><%= user.email %></h3>
    

            <form action="/logout" method="post">
                <a href="/logout"><button type="submit">Logout</button></a>
            </form>


        </div>
    </div>

    <script src="/public/js/app.js"></script>

</body>
</html> -->


<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MoneyMate - Your stress-free solution for group expenses.</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        primary: "#2563EB",
                        secondary: "#16A34A",
                        danger: "#DC2626",
                        lightGray: "#F3F4F6",
                    },
                    spacing: {},
                }
            }
        }
    </script>
</head>
<body class="bg-lightGray min-h-screen flex items-center justify-center">

    <div class="w-full max-w-md bg-white rounded-lg shadow-lg p-6">
        <!-- Profile Picture -->
        <div class="flex justify-center mb-4">
            <img class="w-24 h-24 rounded-full" src="/images/uploads/<%= user.profilepic %>" alt="Profile Picture">
        </div>

        <!-- Username and Email -->
        <div class="text-center mb-6">
            <h2 class="text-2xl font-bold text-gray-800"><%= user.username %></h2>
            <h3 class="text-gray-500"><%= user.email %></h3>
        </div>

        <!-- Logout Button -->
        <form action="/logout" method="post" class="flex justify-center">
            <button type="submit" class="bg-primary text-white py-2 px-4 rounded-lg hover:bg-blue-600 transition ease-in-out duration-300">
                Logout
            </button>
        </form>
    </div>

    <script src="/public/js/app.js"></script>

</body>
</html>
