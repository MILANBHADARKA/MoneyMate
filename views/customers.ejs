<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {},
                    spacing: {},
                }
            }
        }
    </script>
</head>
<body>


    <% customers.forEach(customer => { %>
        <a href="/customerdetail/<%= customer._id %>">
            <div class="bg-gray-200 p-4 m-4">
                <h1 class="text-2xl font-bold">Customer Name: <%= customer.name %></h1>  

                <a href="/editcustomer/<%= customer._id %>"><button class="bg-blue-500 text-white p-2 m-2">Edit</button></a>
                <a href="/deletecustomer/<%= customer._id %>"><button class="bg-red-500 text-white p-2 m-2">Delete</button></a>
            </div>
        </a>
    <% }) %>


    <a href="/addcustomer"><button class="bg-blue-500 text-white p-2 m-2">Add Customer</button></a>

    <script src="/public/js/app.js"></script>
</body>
</html> -->


<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Customer List</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        primary: "#2563EB",
                        danger: "#DC2626",
                        dark: "#1E293B",
                        light: "#F3F4F6",
                    },
                }
            }
        }
    </script>
</head>
<body class="bg-gray-100 font-sans leading-normal tracking-normal">

    <!-- Customer List Container -->
    <div class="container mx-auto px-4 py-8 max-w-5xl">

        <!-- Loop through each customer and display their details in a card -->
        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
            <% customers.forEach(customer => { %>
                <div class="bg-white shadow-md rounded-lg p-6 transition-transform transform hover:scale-105 hover:shadow-xl">
                    <a href="/customerdetail/<%= customer._id %>">
                        <h1 class="text-xl font-semibold text-gray-800 mb-4">Customer: <%= customer.name %></h1>
                    </a>

                    <!-- Action Buttons -->
                    <div class="flex justify-between space-x-2">
                        <a href="/editcustomer/<%= customer._id %>" class="bg-blue-500 hover:bg-blue-600 text-white px-4 py-2 rounded-lg transition-colors">
                            Edit
                        </a>
                        <a href="/deletecustomer/<%= customer._id %>" class="bg-red-500 hover:bg-red-600 text-white px-4 py-2 rounded-lg transition-colors">
                            Delete
                        </a>
                    </div>
                </div>
            <% }) %>
        </div>
    </div>

    <!-- Floating Add Customer Button -->
    <a href="/addcustomer">
        <button class="fixed bottom-8 right-8 bg-blue-500 hover:bg-blue-600 text-white text-3xl font-bold w-14 h-14 rounded-full shadow-lg flex items-center justify-center transition-transform transform hover:scale-110">
            +
        </button>
    </a>

    <script src="/public/js/app.js"></script>
</body>
</html>
